# Suricata binary path. Default is /usr/local/bin/suricata. Can be used to run
# Suricata from different path.
#SURICATA_BIN="/usr/local/bin/suricata"

# Suricata configuration file path. Default is /etc/suricata/suricata.yaml.
# Passed to -c option at startup.
#SURICATA_CONFIG="/etc/suricata/suricata.yaml"
